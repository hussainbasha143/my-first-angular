import { MySqrtPipePipe } from './my-sqrt-pipe.pipe';

describe('MySqrtPipePipe', () => {
  it('create an instance', () => {
    const pipe = new MySqrtPipePipe();
    expect(pipe).toBeTruthy();
  });
});
